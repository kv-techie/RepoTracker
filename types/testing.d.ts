// jest-dom matchers (toBeInTheDocument, toHaveAttribute, ...) for the test files.
// Previously pulled in by jest.setup.ts, which was removed with the duplicate Jest config.
import '@testing-library/jest-dom';
